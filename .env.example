# Optional for UP-04 ACS affordability. If absent, ACS remains proposed and the app still builds.
CENSUS_API_KEY=

# Optional if using HUD APIs instead of documented bulk downloads.
HUD_API_KEY=

# Optional but recommended for NYC Open Data / Socrata pulls.
NYC_APP_TOKEN=

# Optional if an Eviction Lab data request grants private download parameters.
EVICTION_LAB_DATA_URL=
